＊＊git学習＊＊

１.前提学習
    ・Linuxコマンド
    ・Vim
2.コマンド集
    .mkdir フォルダ名{フォルダ作成}
    .git init(カレントディレクトリをgitで使用する宣言)
    .git add HOGE.拡張子（ステージングエリアにあげる）
    .git commit(コミットするコマンド)＊この後にコメントを打ち込む＊
    .git log(コミットできたか確認するコマンド)
    　∟ git log --oneline(ざっくりログ確認)
    　∟ git log -p(具体的にどこが変更したか確認できる)
    　∟ git log --stat(どのファイルに変更があったか確認できる)
3.
4.
5.




